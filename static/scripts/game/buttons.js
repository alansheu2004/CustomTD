function Button(state, inBounds, action, active, validationCanvas) {
    this.state = state;
    this.inBounds = inBounds;
    this.action = action;
    this.active = active;
    this.validationCanvas = validationCanvas;
}